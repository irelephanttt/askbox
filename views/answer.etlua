<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/static/style.css">
        <title>Answer <%= ask.author %>'s ask</title>
        
    </head>
    <body>
        <% render("views.navbar") %>
        <main id="main" class="content">
            <div class="post-card">
                <div class="ask-bubble">
                    <p>Replying to: <b><%= ask.author %>'s</b> ask. </p>
                    <blockquote> <%= ask.content %> </blockquote>
                </div>
                <% if not ask.answered then %>
                    <form class="answer-form" method="POST" action="/answer/<%= ask.id %>">
                        <textarea id="answer-textarea" name="askanswer"></textarea>
                        <button type="submit">Answer!</button>
                    </form>
                <% else %>
                    <p> You have already answered this ask (unless something went wrong) </p>
                <% end %>
            </div>
        </main>
    </body>
</html>

            